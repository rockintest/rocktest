- step: display
  value: Step=${step}

- step: assert.equals
  desc: Check builtin var step
  params:
    expected: 2
    actual: ${step}

- step: assert.equals
  desc: Check builtin var module
  params:
    expected: varBuiltin
    actual: ${module}

- step: assert.equals
  desc: Check builtin var step
  params:
    expected: 4
    actual: ${step}

