# First part is main by default

- type: display
  value: main called with param ${question}

- type: return
  value: "ret = ${question} It's RockTest"

## Functions

- type : function
  name : func1
  steps:
  - type: display
    value : func1 called with param ${param}

  - type: return
    value: ret = func1 returns Hello

## Another function

- type : function
  name : func2
  steps:

  - type: display
    value : func2 called with param ${param}

  - type: return
    value: ret = func2 returns Hello
